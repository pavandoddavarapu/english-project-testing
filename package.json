{
  "name": "speak-up-english-practice",
  "version": "1.0.0",
  "description": "English speech practice app with AI topics and picture talk",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "vercel-build": "node scripts/build.js",
    "dev": "node scripts/build.js && vercel dev",
    "deploy": "node scripts/build.js && vercel"
  },
  "dependencies": {
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "firebase-admin": "^12.1.0",
    "vercel": "^37.0.0"
  }
}